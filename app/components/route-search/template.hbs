{{paper-input
  class="flex-30"
  label="Search Forli"
  value=term
  onChange=(action 'debouncedSearch' (mut term))
  onEnter=(action "searchOnEnter")
}}
{{#unless isLoading}}
  {{#if showResultsCheck}}
    {{route-home discussion=searchResults}}
  {{else if termPresent}}
  {{/if}}
{{/unless}}
