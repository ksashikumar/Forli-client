import underscoreToCamelcase from 'forli/utils/underscore-to-camelcase';
import { module, test } from 'qunit';

module('Unit | Utility | underscore to camelcase');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = underscoreToCamelcase();
  assert.ok(result);
});
